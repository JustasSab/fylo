import React from "react";
import './subscription.css';

const Subscription = () => {
    return (
        <div className="subscription">
            Subscription
        </div>
    );
};

export default Subscription;